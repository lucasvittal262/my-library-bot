apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-lib-bot.fullname" . }}
  labels:
    {{- include "my-lib-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-lib-bot.selectorLabels" . | nindent 4 }}
