apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-lib-bot.fullname" . }}-test-connection"
  labels:
    {{- include "my-lib-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-lib-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
